// ==================== Config.gs — ЛИЧНЫЕ НАСТРОЙКИ ====================
//
// Это ШАБЛОН. В проекте Apps Script создай файл с именем Config.gs
// (Файлы → + → Скрипт → назвать "Config"), скопируй туда содержимое
// этого файла и впиши свои реальные значения.
//
// Этот файл при обновлениях бота НЕ трогается — обновляется только Bot.gs.
// Поэтому токены больше не затираются при вставке нового кода.

// Токен бота — от @BotFather
const TELEGRAM_BOT_TOKEN = 'ВАШ_ТОКЕН_БОТА';

// ID группы книжного клуба (для групп начинается с минуса, например -1001234567890)
const TELEGRAM_CHAT_ID = 'ВАШ_CHAT_ID';

// Любая длинная случайная строка. Используется, чтобы отличать реальные
// запросы Telegram от посторонних обращений на публичный адрес вебхука.
const TELEGRAM_WEBHOOK_SECRET = 'ВАШ_СЕКРЕТ_ВЕБХУКА';

// Адрес веб-приложения из Deploy → Manage deployments (вида
// https://script.google.com/macros/s/AKfycb.../exec, без ?secret=...).
// Нужен только для setupTelegramWebhook() — чтобы запускать её без аргументов.
const WEB_APP_URL = 'ВАШ_URL_ВЕБ_ПРИЛОЖЕНИЯ';
